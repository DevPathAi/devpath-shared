# 세션 핸드오프 — 2026-06-13

> **목적**: 본 세션의 전체 작업을 다음 세션으로 이관한다. 현재 상태·다음 단계·미결/주의사항을 한 곳에 정리한다.
> **원칙**: [절대 조건](https://github.com/DevPathAi/devpath-shared/blob/main/CLAUDE.md) — 추측 금지 / 테스트 우선 / 문제 시 코드 분석. 본 문서의 모든 상태는 실제 파일·CI·커밋으로 검증됨.

---

## 1. 본 세션 완료 작업

| # | 작업 | 결과물 |
|---|------|--------|
| 1 | **레포 환경 구축** | 14개 레포(devpath-* 9 + prototype + workflow-dashboard/guide + documents + .github 등) |
| 2 | **CLAUDE.md 절대조건** | 17개 레포에 "추측 금지·테스트 우선·코드 분석" 명문화 |
| 3 | **prototype** | Vite + React + TS 스캐폴드 |
| 4 | **workflow-dashboard** | project-dashboard 스킬 기반(React/Vite/Tailwind), 17_스케줄 W1~W24 작업 111개 시드, 상세 페이지 첫 작업주차 자동선택. Pages 배포 |
| 5 | **workflow-guide** | VitePress 재구성(사이드바·검색·다크모드·코드 하이라이트), documents 링크 재작성. Pages 배포 |
| 6 | **Synapse 트러블슈팅 참고** | documents/24 — 동일 스택 선행 프로젝트 78건 교훈 |
| 7 | **W1 인프라 (브레인스토밍→스펙→계획→TDD→PR)** | 아래 §2 |
| 8 | **문서 정합성 점검** | documents/25 + 조치 7개(D1~D7) |
| 9 | **작업본 문서 추가** | documents 26~34 + HTML 4종 (개발·사업·컴플라이언스) |

## 2. W1 인프라 — 완료·머지됨

**핵심 결정 (사용자 승인)**: MySQL → **PostgreSQL** (SSOT 5432 + pgvector 5433) / shared → **GitHub Packages(Maven)** / 스키마 **중앙집중**(shared가 Flyway 소유, 서비스는 `ddl-auto: validate`).

- **스펙**: `docs/superpowers/specs/2026-06-13-w1-infra-redefinition-design.md`
- **계획**: `docs/superpowers/plans/2026-06-13-w1-infra.md`
- **결과**: 8개 PR(전 레포 #1) **Rebase Merge 완료**, main CI 전부 green, shared GitHub Packages publish 작동, gitops kustomize 검증 CI(azure/setup-kubectl) 추가.
- **W1 스키마**: 공통 규약(`set_updated_at`) + users 최소 골격 + 법적 분리보관(`dormant_user_archives`, 정보통신망법 §29).

## 3. 현재 상태 (검증됨)

- **모든 레포 main 브랜치, clean** (`.omc/`만 untracked — 커밋 대상 아님).
- **CI**: 8개 레포 main green. ci.yml에 postgres:17 service container + DB_URL env, 서비스는 `packages: read` + GITHUB_TOKEN으로 cross-repo shared 의존 해석.
- **documents**: 01~34 + HTML 4종. MySQL 잔존은 의도된 것만(고아 3종 배너·24 이력·25 점검·23/26/lcs PII 마스킹 예시).
- **로컬 인프라**: docker-compose(PG 2개·Redis·ES·Kafka). 현재 postgres 컨테이너 가동 가능.

## 4. 다음 단계 — W2 (OAuth2)

17_스케줄 Week 2: **Spring Security 7 + OAuth2 Client** (GitHub/Google/카카오 Provider), JWT + Refresh Cookie, `users`·`user_oauth_identities`·`user_profiles` 스키마 확장(W1의 users 골격 위에), `UserRegisteredEvent` Outbox.

- 대상 레포: `devpath-platform-svc`(user/auth) + `devpath-gateway`(OAuth2 엣지) + `devpath-shared`(이벤트·스키마)
- 권장 흐름: 브레인스토밍 → 스펙 → 계획 → TDD (W1과 동일)
- **외부 의존성**(17_스케줄 §11.2): 카카오/Google OAuth 앱 심사는 Week 1 신청 권장(소요 길음) — 미신청 시 W2 착수 시점에 처리 필요.

## 5. 미결 / 주의사항

| 항목 | 내용 |
|------|------|
| 🔴 **PAT 폐기** | 채팅에 노출됐던 첫 fine-grained 토큰 **즉시 폐기** 필요. 현 Classic PAT(`ghp_`)는 `~/.gradle/gradle.properties`에 있음 |
| ⚠️ **Windows dev 환경** | Testcontainers ↔ Docker Desktop 연결 불가 → 테스트는 **compose postgres(5432) JDBC 직접**(DB_URL env). CI는 service container. GitHub Packages는 **Classic PAT 필수**(fine-grained는 org packages publish 미지원) |
| 📄 **documents 중복 의심 5개** | `files (2)`의 community-spec/pia/microcopy/wireframe-spec은 documents 20/23/18/19와 다른 버전(해시 불일치). 현재 documents가 SSOT. 더 상세한 쪽으로 갱신할지 미결 |
| 📄 **Home.md 색인** | README는 갱신, documents/Home.md 위키 색인은 미동기화 |
| 📄 **AR 작업본** | `D:\Download\files (2)`(비레포)에 "참고설계(v2.0)" 배너 추가됨 |
| 🔧 **이미지 빌드 파이프라인** | 서비스 Docker 이미지 빌드 + gitops deployment SHA 태그 미구현(이미지 파이프라인 단계) |

## 6. 관련 메모리 (다음 세션 자동 로드)

- `repo-structure-direction` — 14개 레포 구조·복제 패턴
- `w1-infra-postgres` — W1 결정·Windows dev 우회
- `synapse-troubleshooting-reference` — 선행 프로젝트 교훈
- `no-guessing-test-first` — 절대 작업 원칙
